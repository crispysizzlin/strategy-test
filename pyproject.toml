[project]
name = "quant-income-strategy"
version = "0.1.0"
description = "Research-backed defined-risk options income strategy selector for Schwab orchestration."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Cursor Agent" }]
license = { text = "MIT" }

[project.scripts]
quant-income-strategy = "quant_income_strategy.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
